class Handler {
    method
    uri
    handler
    constructor(uri, method, handler) {
        this.uri = uri
        this.method = method
        this.handler = handler
    }
}

export default Handler
